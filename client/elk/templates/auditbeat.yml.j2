auditbeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/*.log

output.elasticsearch:
  hosts: ["{{ ELASTICSEARCH_IP }}:9200"]

setup.kibana:
  host: "{{ ELASTICSEARCH_IP }}:5601"
