[Unit]
Description=Filebeat Service
After=network.target

[Service]
ExecStart=/opt/{{ item.0 }}-{{ item.1 }}/filebeat -e -c /opt/{{ item.0 }}-{{ item.1 }}/filebeat.yml
Restart=always
User=root
Group=root

[Install]
WantedBy=multi-user.target
